# production:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day

production:
  refresh_property_ai_summaries:
    class: RefreshPropertyAiSummariesJob
    queue: default
    schedule: every day at 12am America/Los_Angeles
